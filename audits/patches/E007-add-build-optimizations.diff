--- a/next.config.js
+++ b/next.config.js
@@ -3,6 +3,7 @@ const path = require('path');
 
 const nextConfig = {
   reactStrictMode: true,
+  poweredByHeader: false, // Remover header X-Powered-By
   swcMinify: true,
   // Configuración para Netlify Functions
   output: 'standalone',
@@ -10,6 +11,7 @@ const nextConfig = {
   images: {
     domains: ['localhost'],
     unoptimized: true, // Necesario para Netlify
+    formats: ['image/webp', 'image/avif'], // Optimizar formatos de imagen
   },
   eslint: {
     dirs: ['src'],
@@ -18,6 +20,7 @@ const nextConfig = {
   typescript: {
     // Habilitar verificación de tipos para mejor calidad
     ignoreBuildErrors: false, // Habilitado para mejor calidad de código
+    tsconfigPath: './tsconfig.json', // Especificar path de tsconfig
   },
   webpack: (config, { dev, isServer }) => {
     // Configuración mínima para evitar errores de chunks
@@ -35,6 +38,7 @@ const nextConfig = {
       };
     }
     
+    // Optimizaciones adicionales
     return config;
   },
