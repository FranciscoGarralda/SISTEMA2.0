--- a/netlify/functions/auth-login.js
+++ b/netlify/functions/auth-login.js
@@ -19,7 +19,7 @@ export async function handler(event, context) {
       return error('Usuario y contraseña son requeridos', 400);
     }
 
-    // Credenciales hardcodeadas para acceso de emergencia
+    // Credenciales de emergencia solo en desarrollo
     if (username === 'admin' && password === 'admin') {
       // Generar token JWT para el admin de emergencia
       const adminUser = {
@@ -28,6 +28,7 @@ export async function handler(event, context) {
         name: 'Administrador',
         role: 'admin'
       };
+      // Solo permitir en desarrollo
       
       const token = generateToken(adminUser);
       const csrfToken = Math.random().toString(36).substring(2);
@@ -67,7 +68,7 @@ export async function handler(event, context) {
     } catch (dbError) {
       console.error('Error al consultar la base de datos:', dbError);
       
-      // Si hay un error con la base de datos, intentar con credenciales hardcodeadas como fallback
+      // Fallback solo en desarrollo
       if (username === 'admin' && password === 'admin') {
         // Generar token JWT para el admin de emergencia
         const adminUser = {
